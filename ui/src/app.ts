import express from 'express';import session from 'express-session';import helmet from 'helmet';import createError from 'http-errors';import {resolve} from 'node:path';import {config} from './config.js';import {configurePassport,passport} from './auth/passport.js';import {requestContextMiddleware} from './middleware/request-context.js';import {csrfTokenMiddleware} from './middleware/csrf.js';import {pageContextMiddleware} from './middleware/page-context.js';import {createAuthRouter} from './routes/auth-routes.js';import {createPageRoutes} from './routes/page-routes.js';import {createSysBORoutes} from './routes/sysbo-routes.js';import {uiErrorHandler} from './error-handler.js';
configurePassport();export function createUiApp(){const app=express();app.disable('x-powered-by');if(config.NODE_ENV==='production')app.set('trust proxy',1);app.use(helmet({contentSecurityPolicy:false}));app.set('view engine','ejs');app.set('views',resolve(process.cwd(),'views'));app.use(express.urlencoded({extended:true}));app.use(express.json());app.use('/assets',express.static(resolve(process.cwd(),'public/assets')));app.use('/css',express.static(resolve(process.cwd(),'public/css')));app.use('/js',express.static(resolve(process.cwd(),'public/js')));app.use('/vendor/bootstrap',express.static(resolve(process.cwd(),'node_modules/bootstrap/dist')));app.use('/vendor/bootstrap-icons',express.static(resolve(process.cwd(),'node_modules/bootstrap-icons/font')));app.use(requestContextMiddleware);app.use(session({name:'manatos.sid',secret:config.SESSION_SECRET,resave:false,saveUninitialized:false,rolling:true,cookie:{httpOnly:true,secure:config.NODE_ENV==='production',sameSite:'lax',maxAge:config.SESSION_IDLE_TIMEOUT_MINUTES*60000}}));app.use(passport.initialize());app.use(csrfTokenMiddleware);app.use(pageContextMiddleware);app.use('/auth',createAuthRouter());app.use('/bo',createSysBORoutes());app.use('/',createPageRoutes());app.use((_q,_r,n)=>n(createError(404,'The requested page was not found.')));app.use(uiErrorHandler);return app;}
