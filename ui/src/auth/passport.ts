import passport from 'passport';import {Strategy as GoogleStrategy} from 'passport-google-oauth20';import {Strategy as FacebookStrategy} from 'passport-facebook';import {config} from '../config.js';
export interface ExternalProfile{provider:string;providerSubject:string;email:string;emailVerified:boolean;displayName?:string;firstName?:string;lastName?:string}
/** Passport handles provider protocol only; account linking/creation remains our logic. */
export function configurePassport(){if(config.GOOGLE_CLIENT_ID&&config.GOOGLE_CLIENT_SECRET&&config.GOOGLE_CALLBACK_URL)passport.use(new GoogleStrategy({clientID:config.GOOGLE_CLIENT_ID,clientSecret:config.GOOGLE_CLIENT_SECRET,callbackURL:config.GOOGLE_CALLBACK_URL},(_a,_r,p,done)=>{const email=p.emails?.[0]?.value;if(!email)return done(new Error('Google did not supply an email.'));done(null,{} as Express.User,{externalProfile:{provider:'google',providerSubject:p.id,email,emailVerified:true,...(p.displayName?{displayName:p.displayName}:{}),...(p.name?.givenName?{firstName:p.name.givenName}:{}),...(p.name?.familyName?{lastName:p.name.familyName}:{})}});}));if(config.FACEBOOK_CLIENT_ID&&config.FACEBOOK_CLIENT_SECRET&&config.FACEBOOK_CALLBACK_URL)passport.use(new FacebookStrategy({clientID:config.FACEBOOK_CLIENT_ID,clientSecret:config.FACEBOOK_CLIENT_SECRET,callbackURL:config.FACEBOOK_CALLBACK_URL,profileFields:['id','displayName','name','emails']},(_a,_r,p,done)=>{const email=p.emails?.[0]?.value;if(!email)return done(new Error('Facebook did not supply an email.'));done(null,{} as Express.User,{externalProfile:{provider:'facebook',providerSubject:p.id,email,emailVerified:false,...(p.displayName?{displayName:p.displayName}:{}),...(p.name?.givenName?{firstName:p.name.givenName}:{}),...(p.name?.familyName?{lastName:p.name.familyName}:{})}});}));}
export function configuredProviders(){const a:{key:string;label:string;icon:string}[]=[];if(config.GOOGLE_CLIENT_ID&&config.GOOGLE_CLIENT_SECRET)a.push({key:'google',label:'Google',icon:'bi-google'});if(config.FACEBOOK_CLIENT_ID&&config.FACEBOOK_CLIENT_SECRET)a.push({key:'facebook',label:'Facebook',icon:'bi-facebook'});return a;}export {passport};
