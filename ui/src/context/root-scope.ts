import type {Request} from 'express';import {operationContext,type SysBOApplication,type SysBOUser} from '@manatos/shared';
export interface RootScope{session:{id:string;authenticated:boolean;authenticationMethod?:string;idleTimeoutMinutes:number};user:{user:SysBOUser}|null;request:{requestId:string;method:string;path:string};workspace:{detailsPanelVisible:boolean;application?:{sysApplicationId:string;sysApplication:SysBOApplication}}}
/** Complete transient scope tree supplied to every page; never persisted as business data. */
export function buildRootScope(req:Request,user:SysBOUser|null,app?:SysBOApplication,idle=30):RootScope{return{session:{id:req.sessionID,authenticated:Boolean(user),...(req.session.authenticationMethod?{authenticationMethod:req.session.authenticationMethod}:{}),idleTimeoutMinutes:idle},user:user?{user}:null,request:{requestId:operationContext.getRequestId(),method:req.method,path:req.path},workspace:{detailsPanelVisible:false,...(app?{application:{sysApplicationId:app.id,sysApplication:app}}:{})}};}
