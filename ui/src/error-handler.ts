import type {ErrorRequestHandler} from 'express';import createError from 'http-errors';import {AppError} from '@manatos/shared';import {addSessionError} from './errors/session-error-log.js';import {renderPage} from './render.js';
/** HTTP errors get pages; AppErrors stay in the shell and appear as a popup. */
export const uiErrorHandler:ErrorRequestHandler=async(e,q,res,_n)=>{if(e instanceof AppError){const entry=addSessionError(q,e);await renderPage(res,'pages/home',{title:'Home',applicationError:e,errorEntry:entry});return;}if(createError.isHttpError(e)){res.status(e.status);await renderPage(res,'errors/http-error',{title:`${e.status} ${e.name}`,httpError:e});return;}const x=new AppError('UNEXPECTED_ERROR',e instanceof Error?e.message:String(e),'An unexpected application error occurred.',true,{cause:e});addSessionError(q,x);res.status(500);await renderPage(res,'errors/http-error',{title:'500 Internal Server Error',httpError:createError(500,'An unexpected UI error occurred.')});};
