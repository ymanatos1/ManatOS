import type {RequestHandler} from 'express';import createError from 'http-errors';import {SysUserRole} from '@manatos/shared';export const requireSignedIn:RequestHandler=(q,_r,n)=>{if(!q.session.userId){n(createError(401,'Please sign in to continue.'));return;}n();};export const requireAdmin:RequestHandler=(_q,r,n)=>{const u=r.locals.currentUser as import('@manatos/shared').SysUser|null;if(!u||u.role!==SysUserRole.Admin){n(createError(403,'Administrator access is required.'));return;}n();};
