import 'dotenv/config';import {z} from 'zod';const opt=z.string().trim().transform(v=>v||undefined).optional();const s=z.object({NODE_ENV:z.enum(['development','test','production']).default('development'),UI_PORT:z.coerce.number().int().positive().default(3001),API_BASE_URL:z.string().url().default('http://localhost:3000'),INTERNAL_API_KEY:z.string().min(8),SESSION_SECRET:z.string().min(16),SESSION_IDLE_TIMEOUT_MINUTES:z.coerce.number().positive().default(30),SESSION_ERROR_LOG_MAX_ENTRIES:z.coerce.number().int().positive().default(20),SHOW_TECHNICAL_ERROR_DETAILS:z.string().default('false').transform(v=>v==='true'),GOOGLE_CLIENT_ID:opt,GOOGLE_CLIENT_SECRET:opt,GOOGLE_CALLBACK_URL:opt,FACEBOOK_CLIENT_ID:opt,FACEBOOK_CLIENT_SECRET:opt,FACEBOOK_CALLBACK_URL:opt});export const config=s.parse(process.env);
