# ManatOS hosted entity-entry popup patch

Apply these files over the current source tree.

This patch adds the generic hosted metadata-driven entry popup used by `Add entry...` and hierarchy-node view, caller create defaults/UI overrides, User -> Person Principal create-related metadata, Account reference presentation cleanup, and the requested Duration/Version sizing adjustments.

No source-file deletions are required.

After extraction run the authoritative workflow:

    npm run format-verify-run
