{
  "name": "manatos-express-platform",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "shared",
    "api",
    "ui"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace shared\" \"npm run dev --workspace api\" \"npm run dev --workspace ui\"",
    "dev:api": "npm run dev --workspace api",
    "dev:ui": "npm run dev --workspace ui",
    "build": "npm run build --workspace shared && npm run build --workspace api && npm run build --workspace ui",
    "test": "npm run test --workspace api && npm run test --workspace ui",
    "verify": "npm run build && npm run test",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "reset:data": "node scripts/reset-data.mjs"
  },
  "devDependencies": {
    "@eslint/js": "^9.34.0",
    "@types/nodemailer": "^8.0.1",
    "concurrently": "^9.2.0",
    "eslint": "^9.34.0",
    "globals": "^16.3.0",
    "prettier": "^3.6.2",
    "typescript-eslint": "^8.41.0"
  },
  "dependencies": {
    "nodemailer": "^9.0.5"
  }
}
