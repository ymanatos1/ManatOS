# Field Components — Detailed Behavior

The canonical UI Field Components guide has moved to [`docs/Field-Components-Detail.md`](Field-Components-Detail.md).

```text
[A] Canonical field type
[B] entity-field dispatcher
[C] One concrete field component
[D] Resolved value source

Flow:
  A --> B
  B --> C
  D --> C
```

Use [`docs/README.md`](README.md) as the UI documentation index.
