/**
 * Public API/runtime version information.
 *
 * API_VERSION identifies the HTTP contract family exposed in route paths.
 * IMPLEMENTATION_VERSION identifies this deployed API implementation/build.
 * Keep IMPLEMENTATION_VERSION aligned with api/package.json when releasing.
 */
export const API_VERSION = 'v1';
export const API_IMPLEMENTATION_VERSION = '0.1.0';
