import {allSysBOMetadata,type SysBOFieldMetadata,type SysBOMetadata} from '@manatos/shared';
const f=(x:SysBOFieldMetadata):Record<string,unknown>=>x.type==='boolean'?{type:'boolean'}:x.type==='number'?{type:'number'}:x.type==='guid'?{type:'string',format:'uuid'}:x.type==='email'?{type:'string',format:'email'}:x.type==='date'?{type:'string',format:'date-time'}:x.type==='enum'?{type:'string',enum:x.enumValues}:{type:'string'};
const schema=(m:SysBOMetadata<unknown>)=>{const properties:Record<string,unknown>={};const required:string[]=[];for(const x of Object.values(m.fieldDefinition)){if(x.sensitive)continue;properties[x.key]=f(x);if(x.required&&!x.generated)required.push(x.key);}if(m.key==='sys-users')properties.hasPassword={type:'boolean'};return{type:'object',properties,required};};
export function buildOpenApiSpec(){const schemas:Record<string,unknown>={};for(const m of Object.values(allSysBOMetadata))schemas[m.name]=schema(m as SysBOMetadata<unknown>);return{openapi:'3.1.0',info:{title:'ManatOS System API',version:'0.1.0',description:'Metadata-driven versioned REST API.'},components:{schemas},paths:{'/api/v1/SysUsers':generic('SysUser'),'/api/v1/SysPrincipals':generic('SysPrincipal'),'/api/v1/SysApplications':generic('SysApplication'),'/api/v1/SysLicenses':generic('SysLicense')}};}
const generic=(n:string)=>({get:{summary:`List ${n} entries`,responses:{'200':{description:'OK'}}},post:{summary:`Create ${n}`,responses:{'201':{description:'Created'},'409':{description:'Unique field conflict'}}}});
