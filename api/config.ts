/**
 * Compatibility re-export.
 *
 * Runtime API configuration lives in src/config.ts. This file remains only
 * to avoid stale tooling/import references outside the compiled src tree.
 */
export { config } from './src/config.js';
